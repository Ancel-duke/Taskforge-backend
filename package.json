{
  "name": "taskforge-backend",
  "version": "1.0.0",
  "description": "TaskForge backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js --ignore node_modules --ignore frontend --ignore logs --ignore uploads --ignore .git",
    "dev:fast": "NODE_ENV=development nodemon server.js --ignore node_modules --ignore frontend --ignore logs --ignore uploads --ignore .git --ignore .next",
    "build": "echo 'No build step required for Node.js'",
    "postinstall": "echo 'Post-install complete'"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.3",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "keywords": [
    "taskforge",
    "api",
    "express",
    "mongodb",
    "socket.io"
  ],
  "author": "TaskForge Team",
  "license": "MIT"
}
